#include "main.h"
/* _puts -> function string parameter*/
void _puts(char *str)
#include<stdio.h>
/**
 * main - Entry point
 * _puts -> function string parameter
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
